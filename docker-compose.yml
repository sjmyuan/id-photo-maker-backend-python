services:
  backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ${MODELS_PATH:-./models}:/app/models
    environment:
      - BG_REMOVAL_MODEL=birefnet-portrait
      - CORS_ORIGIN=
    restart: unless-stopped
